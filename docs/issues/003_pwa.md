- Follow up with having the app being a PWA
- that includes a service worker for offline functionality
- Offline support should improve user experience by being faster in responding. Loading data in the background while the user is using the app. Optimistic changes. Routing needs to be optimized for offline usage and faster.
- have an icon that is a lowercase m in monospace font with a darkgreen background and a beige font color
- Vercel Speed Insights says it has a FCP of 4.3s, LCP of 4.3s, and TTFB of 2.5s.
- allows to install it on macOS and android devices
-
